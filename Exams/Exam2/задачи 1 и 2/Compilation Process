Първа стъпка от процеса на компилация е preprocessing. Препроцесора минава през сорс файловете и изпълнява
всички statement-и, които започват с '#', като #include, #define, #pragma. 
Втора стъпка е компилация. Това е процеса, при който компилатора превръща сорс кода на всеки файл в
асемблерни инструкции.
Трета стъпка е превръщането на тези асемблерни инструкции в машинен код.
Четвърта стъпка е linking. При нея линкера минава и обединява .obj файловете в един краен изпълним файл.
При този етап външни функции или променливи, които се използват в даден .obj файл биват map-нати там където
са декларирани.

Разликата между dynamic и static библиотеките, е че при static библиотеките функциите и променливи, които се
използват от програмата биват съединени с крайния изпълним файл, затова големината му се увеличава.
При dynamic библиотеките функциите и променливите биват заредени по време на изпълнение на програмата.